[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
timeout = 30
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    browser: Tests requiring Playwright browser (slow)